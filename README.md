This is a project to understand push and pull mechanism of Git.

command to make chages - esc+i
command to write the changes is esc + : + w
command to save and get out of insert mode is esc + : + q
if you don't want to save your changes then use esc + q!

# Gitclient
# Gitclinet
